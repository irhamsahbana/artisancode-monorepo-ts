services:
  postgres:
    image: postgres:18-alpine
    container_name: sobatbisnis-db
    restart: unless-stopped
    ports:
      - "7070:5432"
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
      POSTGRES_DB: ${POSTGRES_DB:-sobatbisnis}
    volumes:
      - postgres_data:/var/lib/postgresql

  redis:
    image: redis:8-alpine
    container_name: sobatbisnis-redis
    restart: unless-stopped
    ports:
      - "7071:6379"
    volumes:
      - redis_data:/data

volumes:
  postgres_data:
  redis_data:
