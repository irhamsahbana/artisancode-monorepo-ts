// ponytail: single switch for the UI-only demo. Flip to `false` to restore the
// real backend calls (every service branches on this). Kept as `boolean` (not a
// literal) so neither branch looks "unnecessary" to a reader or linter.
export const DEMO_MODE = true;
